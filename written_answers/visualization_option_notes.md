# Map

* Including the FIPS code to map to the Geojson in datawrapper.
* Snake case column names
* Additional fields included for programmatic tool tip.
* Data date is included for subtitle.
* Run date is included for notes.

# Table

* Sorting descending on the overall `unemployment_rate`
* `mom_change_pp` can be heatmap or small bar chart inside the table column
* Including both `census_region` and `census_division` so states can be grouped by them in the table.
* Data date is included for subtitle.
* Run date is included for notes.